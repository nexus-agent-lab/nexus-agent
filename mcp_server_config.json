{
    "mcpServers": {
        "demo_server": {
            "command": "python",
            "args": [
                "/app/servers/demo_tool.py"
            ],
            "required_role": "admin"
        }
    },
    "_comment": "required_role is an extensible string identifier. Currently validates against User.role. In future versions, this can map to granular permission keys (e.g., 'can_access_finance')."
}